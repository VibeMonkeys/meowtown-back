# 가장 간단한 단일 스테이지 Dockerfile (문제 해결용)
FROM openjdk:21-jdk-slim

# 작업 디렉토리 설정
WORKDIR /app

# Gradle Wrapper 복사 및 실행 권한 부여
COPY gradlew ./
COPY gradle gradle/
RUN chmod +x gradlew

# 빌드 파일 복사
COPY build.gradle gradle.properties ./

# 소스 코드 복사
COPY src src/

# 애플리케이션 빌드
RUN ./gradlew bootJar --no-daemon

# 포트 노출
EXPOSE 8080

# 애플리케이션 실행
CMD ["java", "-jar", "build/libs/*.jar"]